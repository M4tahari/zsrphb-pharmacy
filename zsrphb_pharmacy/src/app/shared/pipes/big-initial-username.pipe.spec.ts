import { BigInitialUsernamePipe } from './big-initial-username.pipe';

describe('BigInitialUsernamePipe', () => {
  it('create an instance', () => {
    const pipe = new BigInitialUsernamePipe();
    expect(pipe).toBeTruthy();
  });
});
